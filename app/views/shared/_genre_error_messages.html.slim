- if @genre.errors.any?
  #error_explanation
    .alert.alert-error
      |  The form contains
      = pluralize(@genre.errors.count, "error")
      | . 
    ul
      - @genre.errors.full_messages.each do |msg|
        li
          | * 
          = msg
