<!-- - @users.each do |user|
  p 	
	= user.email
	br
	= user.name -->
<% provide(:title, "List of users") %>
<div class="row">  

<ul>
<% @users.each do |user| %>
    	<li><%= link_to user.name, user_path(user) %></li>
  <% end %> 
</ul>
</div>
